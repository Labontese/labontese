---
# monitoring-stack/inventory.yml
# Update with your server details

all:
  children:
    monitoring:
      hosts:
        monitor01:
          ansible_host: 10.0.1.5
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519

    # Add node_exporter to these hosts separately
    targets:
      hosts:
        webserver:
          ansible_host: 10.0.1.10
        dbserver:
          ansible_host: 10.0.1.20
